// Stub — fleshed out in Task 11 (claim detail page: line editor, status
// actions, payer/patient summary). Kept minimal here so routes.tsx and
// tsc stay green while Task 10 lands the worklist + nav + routes.
export function ClaimDetailPage() {
  return null;
}
